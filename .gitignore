# Python
__pycache__/
*.py[cod]
.venv/
.venv311/
.venv*/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Electron
node_modules/
dist/
out/
.vite/
*.log

# Models & large weights (never commit these)
models/
weights/
*.onnx
*.pth
*.pt
*.engine
*.bin

# Env & local config
.env
.env.local
*.local

# OS
.DS_Store
Thumbs.db

# Build & recordings
build/
recordings/
captures/

# Local user data (face profiles etc.)
backend/data/

# Consumer bundle build output (multi-GB)
dist-bundle/

# Local-only Lucy/fal key (never commit, never ship)
.lucy_key.local
backend/.lucy_key.local

# Local-only Flutterwave keys (never commit, never ship)
backend/.flutterwave.local

# Lucy WebRTC probe output frames
lucy_out*.png
_lucy_probe*.py

# Local-only Decart key (never commit, never ship)
backend/.decart.local
